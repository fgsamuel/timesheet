[tool.isort]
line_length = 120
profile = "black"
force_single_line = true
skip_glob = "**/migrations/*.py, .venv/*"

[tool.black]
line-length = 120
target-version = ["py310"]
extend-exclude = '''
(
    /(
        \.venv
        | migrations
        | \.git
    )/
)
'''